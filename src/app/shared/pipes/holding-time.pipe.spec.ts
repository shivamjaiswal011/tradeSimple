import { HoldingTimePipe } from './holding-time.pipe';

describe('HoldingTimePipe', () => {
  it('create an instance', () => {
    const pipe = new HoldingTimePipe();
    expect(pipe).toBeTruthy();
  });
});
